goxy(): check if unsigned char is better than just unsigned
PATH: check if environment functions report errors
change size of context
editing of long command lines that wraps the screen
Test if "command.com A:\" accepts the A:\command.com
nls_time.c: missing free(q) at the end?
IF: MS DOS 6 says: if a,a==a echo OK!!!
